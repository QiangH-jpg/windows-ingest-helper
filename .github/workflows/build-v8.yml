name: Build Windows EXE v8

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install deps
        run: pip install pyinstaller tos
      - name: Download ffmpeg
        run: |
          Invoke-WebRequest -Uri "https://www.gyan.dev/ffmpeg/builds/ffmpeg-release-essentials.zip" -OutFile ffmpeg.zip
          Expand-Archive ffmpeg.zip -DestinationPath ffmpeg_extracted
          $d = Get-ChildItem ffmpeg_extracted -Directory | Select-Object -First 1
          New-Item -ItemType Directory -Force -Path Windows_Release\bin
          Copy-Item "$($d.FullName)\bin\ffmpeg.exe" Windows_Release\bin\
          Copy-Item "$($d.FullName)\bin\ffprobe.exe" Windows_Release\bin\
          Remove-Item ffmpeg.zip, ffmpeg_extracted -Recurse -Force
      - name: Build EXE
        run: pyinstaller --onefile --windowed --name ingest_helper_v8 windows_ingest_helper/main.py
      - name: Copy files
        run: |
          Copy-Item dist\ingest_helper_v8.exe Windows_Release\
          New-Item -ItemType Directory -Force -Path Windows_Release\bin
      - name: Create version.txt
        run: |
          $runId = "${{ github.run_id }}"
          $commitSha = "${{ github.sha }}"
          $buildTime = Get-Date -Format "yyyy-MM-ddTHH:mm:ssZ"
          @"
Windows Ingest Helper - Version Information
============================================

Version: v8
Build Time: $buildTime
GitHub Run ID: $runId
Commit SHA: $commitSha

Changes from v7:
- Rebuilt with fresh artifact
- New TOS key for clean delivery
- Version self-verification added
"@ | Out-File -FilePath Windows_Release\version.txt -Encoding utf8
      - name: Create README.txt
        run: |
          @"
Windows 上传/预处理助手 v8
============================

下载链接：https://e23-video.tos-cn-beijing.volces.com/Windows_Executable/Windows_Ingest_Helper_v8.zip

使用说明：
1. 解压到任意目录
2. 运行 ingest_helper_v8.exe
3. 选择本地视频素材
4. 自动转码 720p proxy
5. 自动上传 TOS
6. 返回 TOS 公共对象 URL

包含文件：
- ingest_helper_v8.exe (主程序)
- bin/ffmpeg.exe (视频转码)
- bin/ffprobe.exe (视频分析)
- version.txt (版本信息)
- README.txt (本文件)

构建信息：
- Run ID: ${{ github.run_id }}
- Commit: ${{ github.sha }}
- 构建时间：$(Get-Date -Format "yyyy-MM-ddTHH:mm:ssZ")
"@ | Out-File -FilePath Windows_Release\README.txt -Encoding utf8
      - name: Create ZIP
        run: Compress-Archive -Path Windows_Release\* -DestinationPath Windows_Ingest_Helper_v8.zip -Force
      - uses: actions/upload-artifact@v4
        with:
          name: windows-release-v8
          path: Windows_Ingest_Helper_v8.zip
      - name: Upload TOS
        if: ${{ env.TOS_AK != '' }}
        run: |
          $tosKey = "Windows_Executable/Windows_Ingest_Helper_v8.zip"
          python -c "
          import os
          from tos import TosClientV2
          TOS_AK = os.getenv('TOS_AK')
          TOS_SK = os.getenv('TOS_SK')
          if not TOS_AK or not TOS_SK:
              print('Error: TOS secrets not configured')
              exit(1)
          client = TosClientV2(ak=TOS_AK, sk=TOS_SK, endpoint='tos-cn-beijing.volces.com', region='cn-beijing')
          client.put_object_from_file(bucket='e23-video', key='$tosKey', file_path='Windows_Ingest_Helper_v8.zip')
          print('Upload successful')
          print('Download URL: https://e23-video.tos-cn-beijing.volces.com/$tosKey')
          "
        env:
          TOS_AK: ${{ secrets.TOS_AK }}
          TOS_SK: ${{ secrets.TOS_SK }}
