# 视频项目独立化分界线（2026-04-11）

本项目在此日期完成"独立化整理"：

**整理前**：
- 寄居在 OpenClaw 工作区
- 路径写死 /home/admin/.openclaw/
- 依赖 OpenClaw 环境
- 部署方式不明确

**整理后**：
- 独立项目目录结构
- 配置模板化，支持环境变量
- 不依赖 OpenClaw 路径
- 完整部署资产（systemd、nginx、脚本）

**规则**：
1. 禁止在业务代码中写死 OpenClaw 路径
2. 正式生产入口只能有一个（run.py）
3. 测试/修复脚本必须与主链隔离
4. 所有部署方式必须进入 deploy/ 与 config/

---

# PROJECT_STATE.md - 视频项目状态

**当前阶段**: 独立化整理完成阶段
**独立化日期**: 2026-04-11

---

## 主链状态

- V1：素材隔离完成（uploads/materials/ 唯一来源）
- V3：timeline 已稳定（固定 3 秒/镜头，总时长对齐音频）
- V3.5：Clip Extraction 已实现（按 timeline 裁剪子片段）
- V4：按 clip 拼接（只拼接 clip_*.mp4，禁止拼接原素材）
- V5：5 道门禁生效（全量 clip 时长 / 字幕完整性 / 结尾校验 / 时长合理性 / 文件大小）

---

## 当前结果

✔ 视频正常（真实素材画面）
✔ 无截断 / 无重复 / 无闪屏
✔ 时长对齐（视频 37.47s vs 音频 37.49s，差 0.02s）
✔ 字幕完整（匹配率 100%）
✔ 词中间拆分已消除（短语保护 + 最长匹配）

---

## 当前禁止

❌ 禁止使用旧 run_*.py（已归档到 legacy/）
❌ 禁止绕过 run_production.py
❌ 禁止回到补丁式开发
❌ 禁止直接拼接原素材（必须通过 V3.5 裁剪）

---

## 下一步

👉 接入 V2 语义选片（基于主链）
👉 V3 节奏优化（短/中/长镜头分布）
👉 V4 字幕样式增强
