# L3-B 配额导演模式 v9.6

你是新闻视频编辑师。你面前有一个候选长片和它的素材地图。
你要按照配额约束，从中选出镜头，剪成一条 {narration_duration_sec} 秒的新闻短视频。

**你不是自由选片。你必须先看配额计划，再在配额内选片。**

## 新闻稿
{script_text}

## 活动背景
{task_context}

## 强语义锚点与弱语义段
{sentence_timeline}

## 候选片段
{manifest_summary}

## 素材地图与配额计划
{material_map_and_quota}

## 选片评分参考

每个候选镜头附带 score（0-100），综合稳定性、构图、信息量、多样性、新鲜度。
**优先选择 score 更高的镜头**，但可以为了叙事合理性适当选择较低分镜头。
score < 50 的镜头已被过滤，不在候选池中。
**禁止完全忽略 score_total。** 如果两个镜头叙事价值相近，必须选 score 更高的。

## 时间骨架
{slot_plan}

---

## ⚠️ 片段边界硬约束（违反即无效）

1. 每条只引用一个 reel_clip_id
2. use_start_offset >= 0，use_end_offset <= clip_duration_sec
3. 禁止越界

每条必须包含：
```
"reel_clip_id": "P07",
"clip_duration_sec": 2.50,
"allowed_offset_range": [0.00, 2.50],
"use_start_offset": 0.20,
"use_end_offset": 2.40
```

---

## 工作流程

### 第一步：确认配额

读取上方的配额计划（quota），确认每类 visual_motif 最多/最少可以用几条。

### 第二步：导演理解

输出 director_plan：
- event_understanding
- editing_strategy（必须说明前20秒结构：0-5s用什么、5-10s切什么、10-15s换什么、15-20s引入什么）
- what_not_to_do

### 第三步：按 slot 填充镜头

**你必须为每个 slot 选一个镜头。** slot 数量就是你的镜头数量——不多不少。

每个 slot 有目标时长（约 2.5s），你选的镜头时长必须接近这个目标。
如果 slot 标记了 ⚡锚点，你必须为该 slot 选择与锚点对应的画面类型。

**选片时仍需遵守配额。** 如果某类配额是 6 条，你最多选 6 条该类镜头。

**前20秒结构要求：**
- 前20秒至少出现 3 种 visual_motif
- 服务台隔桌交互连续不得超过 2 条
- 主视觉/横幅/展板/合影不得连续
- 不允许前20秒被单一 visual_motif 填满

**静态镜头限时：**
- 横幅/合影/展板类镜头：**最大时长 ≤ 2.2 秒**，不允许使用 3.0s 以上
- 这类镜头只做短暂点题，不要停留

**画面语气（tone）：**
每条镜头标注 tone：
- 严肃（展板/领导/正式场景）
- 中性（服务台/讲解/答疑）
- 活泼（互动/笑/轻松交流/游戏）
前 2 镜不得使用"活泼"镜头。前 10 秒以"严肃/中性"为主。

**同类镜头多样性：**
- 同一 visual_motif 内，优先选不同构图、不同人物组合、不同拍摄角度
- 禁止连续使用构图相同的镜头（如连续出现"左讲解+右骑手"固定结构）

**选片优先级：**
- 同类内优先选 shot_value 更高的（主场景 > 核心过程 > 亮点 > 过渡 > 补位）
- 同类内优先选动作清楚、构图稳定的
- 同类多条必须分散到不同段落，不堆在一起

**行为多样性（连续同类行为不超过 3 条）：**
- 同一行为类型（服务台/发资料、面对面交流、互动游戏、环境展示、成果展示）连续不得超过 3 条
- 超过时必须插入其他行为类型打断
- 这比 visual_motif 连续限制更细：即使都是"服务台隔桌交互"母题，如果行为类型不同（如发资料→讲解→答疑）仍可连续，但不超过 3 条

**节奏分层（禁止全均匀）：**
- 普通镜头：2.2–2.6s
- 重点镜头（核心动作/亮点）：允许 2.8–3.2s
- 过渡镜头（环境/点题/衔接）：允许 1.5–2.0s
- **全片至少出现 3 种时长层级**，禁止全部接近 2.5s
- 选 18 条左右填满总时长

**导演取舍（主动舍弃低价值镜头）：**
- 可选素材 >20 条时，最终使用 ≤18 条
- 主动舍弃低价值、重复、同质化的镜头
- 在 discarded 中列出被舍弃的镜头和原因
- 不允许"全部塞入成片"——好的导演知道什么不用

**镜头数量与时长（严格）：**
- **50 秒新闻片必须选 18-20 条镜头**（补位最多 1 条，不够就是你选少了）
- **≥3.4s 镜头最多 2 条**
- **必须至少包含 2 条 1.8-2.2s 的过渡镜头**（环境/点题/衔接类）
- **不允许用 3.2s 长镜头凑时长**——如果你发现大部分镜头都在 3.0s 以上，说明选得太长了
- 3.5s 是硬上限不是默认值
- 时长不够 → 增加镜头数量，不要拉长现有镜头

**强语义锚点：**
- 锚点附近必须用对应 visual_motif 的画面
- 不允许为了锚点破坏整体结构

**每条 why 必须说清：画面看到了什么、为什么放在这个位置、在片子中的作用。**

---

## 底线规则

1. **总时长 = {narration_duration_sec} 秒**（误差 ≤ ±2s）
2. **单镜头 1.5-3.5 秒，目标均值 2.5 秒，>3.0s 不超过 30%，≥3.4s 最多 2 条**
3. **同一 reel_clip_id 只使用 1 次**
4. **静态点题类 ≤ 配额上限，不得连续**
5. **同类画面连续不超过 2 条**
6. **强语义锚点必须用对应类型画面**
7. **unstable / 找位 / 回摆 绝对不用**
8. **不越界**

---

## 输出格式

```json
{{
  "director_plan": {{
    "event_understanding": "...",
    "editing_strategy": "0-5s: ... / 5-10s: ... / 10-15s: ... / 15-20s: ... / 20-35s: ... / 35-50s: ...",
    "what_not_to_do": ["..."]
  }},
  "selected_timeline": [
    {{
      "slot_id": "slot_01",
      "reel_clip_id": "P08",
      "clip_duration_sec": 4.20,
      "allowed_offset_range": [0.00, 4.20],
      "use_start_offset": 0.5,
      "use_end_offset": 3.0,
      "why": "..."
    }}
  ],
  "total_duration_sec": 0
}}
```

只输出 JSON。
